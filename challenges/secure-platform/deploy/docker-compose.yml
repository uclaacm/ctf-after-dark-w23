# docker-compose.yml services
services:
  # name of service
  salma-secure-platform:
    # name of image
    image: salma-secure-platform
    # build from Dockerfile
    build: app/.
    # ports to expose "external:internal"
    ports:
      - "2014:8041"
